# 2026-09-21 开发日志

## [00:03:26] GitHub状态面板

- 完成时间：2026-09-21 00:03:26（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `.github/workflows/deploy.yml`
  - `scripts/fetch-github-stats.ts`
  - `scripts/README.md`
  - `src/features/github/types.ts`
  - `src/features/github/utils/githubStats.ts`
  - `src/features/github/utils/githubStats.test.ts`
  - `src/features/github/data/github-stats.json`
  - `src/features/github/hooks/useGithubStats.ts`
  - `src/features/github/components/GithubStatusPanel.tsx`
  - `src/pages/status/index.astro`
  - `src/hooks/useFetchGithubStats.ts`
  - `src/components/widgets/SiteStatsCard.tsx`
  - `docs/实施计划/2026-09-20-GitHub状态面板-实施计划.md`
- 完成内容：
  - 新增公开 GitHub 账户、仓库聚合、博客仓库最新提交与近期活跃仓库的数据同步、静态缓存和状态页面板。
  - 将侧边栏最后活跃时间切换为复用本站静态 GitHub 缓存，移除浏览器对 GitHub API 的直接请求。
  - 在既有 GitHub Actions 指标同步流程中接入 GitHub 缓存更新，并使用 Actions 内置令牌提高 API 配额。
  - 首次成功生成 `github-stats.json`；同步失败且已有缓存时保留旧数据。
- 验证结果：
  - `pnpm exec tsx scripts/fetch-github-stats.ts`：通过，成功写入 GitHub 静态缓存。
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，2/2 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm run build`：通过，退出码 0，生成 `dist/status/index.html` 和 GitHub 状态面板客户端产物。
- 任务状态：已完成

## [08:37:35] GitHub贡献热力图调整

- 完成时间：2026-09-21 08:37:35（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/github/types.ts`
  - `src/features/github/utils/githubStats.ts`
  - `src/features/github/utils/githubStats.test.ts`
  - `src/features/github/components/GithubStatusPanel.tsx`
  - `src/features/github/data/github-stats.json`
  - `scripts/fetch-github-stats.ts`
  - `scripts/README.md`
  - `docs/实施计划/2026-09-20-GitHub状态面板-实施计划.md`
- 完成内容：
  - 将 GitHub 账户/仓库统计面板调整为 Contributions 年度贡献热力图。
  - 增加公开 Contributions SVG 解析，提取每日日期、贡献次数和等级；缓存有效时由本站静态数据绘制热力图。
  - 缓存暂时为空时回退到 GitHub 官方贡献图，避免网络同步完成前页面没有热力图。
  - 同步脚本无法访问 GitHub 时保留旧缓存；本次验证确认旧缓存未被覆盖。
- 验证结果：
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm exec tsx scripts/fetch-github-stats.ts`：退出码 0；当前环境请求 GitHub 失败，按设计保留已有缓存。
  - `pnpm run build`：通过，退出码 0，生成 `dist/status/index.html` 和 `GithubStatusPanel` 客户端产物。
- 任务状态：已完成


## [09:21:01] GitHub贡献热力图Mock数据

- 完成时间：2026-09-21 09:21:01（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/github/data/github-stats.json`
- 完成内容：
  - 为过去 365 天生成临时 GitHub 贡献 Mock 数据，总贡献数为 1499。
  - 覆盖贡献等级 0～4，便于查看低活跃、普通活跃和高活跃状态下的热力图样式。
  - 保留原有账户和仓库信息；后续 GitHub Actions 同步成功后会覆盖 `contributions` 数据。
- 验证结果：
  - JSON 语法校验通过。
  - 数据结构校验通过：365 天、日期升序、等级 0～4 全部存在。
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm run build`：通过，退出码 0，生成 `dist/status/index.html`。
- 任务状态：已完成

## [08:59:01] GitHub贡献热力图改用GraphQL并移除外部图片

- 完成时间：2026-09-21 08:59:01（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/github/types.ts`
  - `src/features/github/utils/githubStats.ts`
  - `src/features/github/utils/githubStats.test.ts`
  - `src/features/github/components/GithubStatusPanel.tsx`
  - `scripts/fetch-github-stats.ts`
  - `scripts/README.md`
  - `docs/实施计划/2026-09-20-GitHub状态面板-实施计划.md`
- 完成内容：
  - 使用 GitHub GraphQL `contributionCalendar` 获取每日贡献数、日期和贡献等级。
  - 将 GraphQL 周数据扁平化为本站缓存格式，热力图继续由 React 格子自行渲染。
  - 删除 GitHub Contributions 外部图片兜底，缓存无每日数据时显示本地提示。
  - 在 GitHub Actions 中使用 `${{ github.token }}`；同步失败时继续保留已有缓存。
- 验证结果：
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm exec tsx scripts/fetch-github-stats.ts`：退出码 0；当前本地环境因 GitHub GraphQL 返回 `403 rate limit exceeded`，按设计保留已有缓存。
  - `pnpm run build`：通过，退出码 0，生成 `dist/status/index.html`。
  - 源码搜索：未发现 Contributions SVG、`data-date`/`data-count`/`data-level` 解析或外部热力图图片引用。
- 任务状态：已完成

## [09:21:01] GitHub贡献热力图Mock数据

- 完成时间：2026-09-21 09:21:01（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/github/data/github-stats.json`
- 完成内容：
  - 为过去 365 天生成临时 GitHub 贡献 Mock 数据，总贡献数为 1499。
  - 覆盖贡献等级 0～4，便于查看低活跃、普通活跃和高活跃状态下的热力图样式。
  - 保留原有账户和仓库信息；后续 GitHub Actions 同步成功后会覆盖 `contributions` 数据。
- 验证结果：
  - JSON 语法校验通过。
  - 数据结构校验通过：365 天、日期升序、等级 0～4 全部存在。
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm run build`：通过，退出码 0，生成 `dist/status/index.html`。
- 任务状态：已完成

## [09:31:11] GitHub贡献热力图样式调整

- 完成时间：2026-09-21 09:31:11（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/github/components/GithubStatusPanel.tsx`
- 完成内容：
  - 参考 WakaTime 热力图调整 GitHub 贡献卡片的标题、日期、网格和图例布局。
  - 将热力图改为固定 7 行、按周列流动的网格，卡片内水平居中并在窄屏支持横向滚动。
  - 统一方格尺寸、间距、圆角和悬停效果；贡献等级颜色改为绿色渐变。
  - 将空贡献颜色调整为低透明度前景色，避免与卡片背景混淆。
- 验证结果：
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm run build`：通过，退出码 0，生成 133 个页面。
- 任务状态：已完成

## [09:39:16] GitHub热力图卡片对齐WakaTime样式

- 完成时间：2026-09-21 09:39:16（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/github/components/GithubStatusPanel.tsx`
- 完成内容：
  - 移除 GitHub 热力图卡片外层的大标题和图标，改为 WakaTime 风格的小号大写卡片标题。
  - 将热力图改为 `flex-wrap` 大方格布局，使用更大的方格和统一间距，填充卡片内容区域。
  - 保留绿色贡献等级和悬浮日期/贡献次数提示。
  - 删除右下角“较少/较多”颜色图例。
  - 将 GitHub 主页、最近活跃时间和同步时间移至卡片下方信息行。
- 验证结果：
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm run build`：通过，退出码 0，生成 133 个页面。
  - 源码检查：未发现“较少/较多”图例文本。
- 任务状态：已完成

## [09:45:31] 修复热力图边缘Tooltip裁切

- 完成时间：2026-09-21 09:45:31（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/features/waka/components/HeatMap.tsx`
  - `src/features/github/components/GithubStatusPanel.tsx`
- 完成内容：
  - 修复 WakaTime 和 GitHub 热力图边缘单元格的悬浮说明被卡片裁切的问题。
  - 为两个热力图卡片覆盖通用 Card 的 `overflow-hidden`，改为 `overflow-visible`，允许 tooltip 超出卡片边界显示。
  - 保留卡片圆角、背景、边框和原有 tooltip 定位及交互样式。
- 验证结果：
  - `pnpm exec vitest run src/features/github/utils/githubStats.test.ts`：通过，4/4 测试通过。
  - `pnpm run check`：通过，0 error；项目原有 24 条 hint 未处理。
  - `pnpm run build`：通过，退出码 0，生成 133 个页面。
  - 源码检查：两个热力图卡片均已使用 `overflow-visible`。
- 任务状态：已完成
