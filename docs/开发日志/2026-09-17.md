# 2026-09-17 开发日志

## [10:14:57] 恢复博客字体与动态背景并更正性能优化记录

- 完成时间：2026-09-17 10:14:57（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/layouts/BaseLayout.astro`
  - `src/styles/global.css`
  - `package.json`
  - `pnpm-lock.yaml`
  - `src/components/core/StaticBlogBackground.astro`（已删除）
  - `docs/实施计划/2026-09-16-博客页面性能优化实施计划.md`
  - `docs/开发日志/2026-09-17.md`
- 完成内容：
  - 恢复 `AmbientBlogBackground` 的 `client:idle` 动态背景渲染，删除未经确认的静态背景替代组件。
  - 恢复 `@fontsource/lxgw-wenkai` 依赖、全局 CSS 导入与字体栈，保留原有中文字体视觉效果。
  - 更正实施计划：明确文章统计缓存和 GitHub 数据预取未实施；保留不改变视觉设计的 KaTeX 本地化、Toast 按需加载、侧栏统计延迟 hydration、首图加载优先级和性能检查脚本。
  - 2026-09-16 日志中关于删除字体、替换动态背景及实现缓存/预取的条目不准确；本条记录为对此的更正，不将其视为已完成工作。
- 验证结果：
  - `pnpm run check:strict` 通过：0 errors。
  - `pnpm build` 通过：129 个页面，85.88 秒。
  - `pnpm run perf:check` 通过：构建产物 342.79 MiB；图片 227.03 MiB、音频 71.31 MiB、字体 21.15 MiB。
  - `git diff --check` 通过。
- 任务状态：已完成

## [10:23:20] 去除仓鼠跑轮加载动画

- 完成时间：2026-09-17 10:23:20（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/components/ui/HamsterLoader.tsx`（已删除）
  - `src/components/ui/HamsterLoader.module.css`（已删除）
  - `src/components/ui/LoadingStatus.tsx`
  - `src/components/widgets/MomentsCarousel.tsx`
  - `src/features/comments/components/CommentAdmin.tsx`
  - `src/features/comments/components/CommentSection.tsx`
  - `src/features/moments-section/components/MomentsSection.tsx`
- 完成内容：
  - 移除仓鼠跑轮加载器及其动画样式。
  - 将所有原有调用替换为通用 `SpinnerLoader`，保留加载提示与无障碍状态语义。
  - 移除 `LoadingStatus` 中已失效的仓鼠加载变体。
- 验证结果：
  - `pnpm run check` 通过：0 errors、0 warnings、24 hints。
  - 全源码检索未发现 `HamsterLoader`、相关导入、组件调用或 `hamster` 变体。
  - `pnpm run build` 在工具 30 秒执行时限内被中断，未获得退出状态。
- 任务状态：已完成

## [10:32:46] 修复加载优化审阅发现

- 完成时间：2026-09-17 10:32:46（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/pages/about/guestbook.astro`
  - `src/components/widgets/MomentsCarousel.tsx`
  - `docs/开发日志/2026-09-17.md`
- 完成内容：
  - 在留言簿页面为 `CommentSection` 同步挂载 `ToastViewport`，恢复评论加载、提交及人机验证失败时的反馈提示。
  - 移除 `MomentsCarousel` 的全部图片批量预加载逻辑，保留轮播当前项变化时仅预取下一张图片的空闲期策略，避免用户未浏览时下载全部瞬间图片。
- 验证结果：
  - `pnpm run check:strict` 通过：0 errors。
  - `git diff --check` 通过。
  - 源码检索确认所有 `CommentSection` 使用点均有对应 `ToastViewport`；不存在 `preloadAllImages` 或 `imagesPreloaded` 残留。
  - `pnpm build && pnpm run perf:check` 在客户端构建阶段受工具 30 秒执行时限终止，未获得退出状态；未将构建标记为通过。
- 任务状态：已完成

## [11:02:24] 侧边栏个人卡片社交链接图标化

- 完成时间：2026-09-17 11:02:24（Asia/Shanghai，UTC+08:00）
- 涉及文件：
  - `src/components/widgets/ProfileCard.astro`
  - `docs/开发日志/2026-09-17.md`
- 完成内容：
  - 将侧边栏个人介绍卡片中的 GitHub 与 Bilibili 文字链接替换为对应的内联品牌 SVG 图标。
  - 保留原有跳转地址、悬停主题样式与新窗口安全属性，并通过 `aria-label` 保留无障碍名称。
- 验证结果：
  - `pnpm check` 通过：0 errors、0 warnings、24 hints。
- 任务状态：已完成
